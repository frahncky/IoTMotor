name: Cloudflare dashboard

on:
  push:
    branches: [main]
    paths:
      - 'dashboard-cloudflare/**'
      - '.github/workflows/cloudflare-dashboard.yml'
  pull_request:
    paths:
      - 'dashboard-cloudflare/**'
      - '.github/workflows/cloudflare-dashboard.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check JavaScript syntax
        run: node --check dashboard-cloudflare/app.js
      - name: Run dashboard smoke tests
        run: node --test dashboard-cloudflare/app.test.cjs
      - name: Check static site files
        run: |
          test -s dashboard-cloudflare/index.html
          test -s dashboard-cloudflare/_headers
          grep -q 'app.js' dashboard-cloudflare/index.html
