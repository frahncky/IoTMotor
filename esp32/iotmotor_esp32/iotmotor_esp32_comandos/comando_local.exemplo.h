#pragma once
// Modelo versionado: NAO ponha a senha aqui, este arquivo vai para o GitHub.
// Copie como comando_local.h nas DUAS pastas de firmware (esse fica no
// .gitignore) e use a mesma senha nas duas placas, no painel e no aplicativo.
//
// Com a senha definida, a placa so obedece a comandos cifrados com ela. Sem
// ela, continua aceitando comando aberto de qualquer um no broker publico.
//
// Escolha algo longo (20 caracteres ou mais). Se perder a senha, a unica saida
// e regravar as placas por cabo.
#define COMANDO_SENHA "troque-por-uma-senha-longa"
