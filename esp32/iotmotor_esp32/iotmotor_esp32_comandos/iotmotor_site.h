#pragma once
// AUTOMATICALLY GENERATED by tools/integrate_profiles.py. Do not edit by hand.
// Same dashboard HTML and scripts on Cloudflare and ESP32 local HTTP server.
const char INDEX_HTML[] PROGMEM = R"IOTHTML(<!doctype html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="theme-color" content="#0b2835"><meta name="description" content="IoTMotor: medições elétricas PZEM e sensores ESP32-S3">
<title>IoTMotor | Comandos e medições</title>
<style>
:root{font:15px/1.5 Inter,system-ui,-apple-system,Segoe UI,sans-serif;color:#eff7fa;background:#091c27}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at 95% 0%,#194553 0,transparent 36%),#091c27}button,input{font:inherit}button{cursor:pointer}button:focus-visible,input:focus-visible{outline:3px solid #ffc46d;outline-offset:3px}h1,h2,h3,p{margin-top:0}h1{font-size:clamp(30px,4vw,49px);letter-spacing:-.04em;margin-bottom:8px}h2{font-size:20px}h3{font-size:15px;margin:0 0 15px}.shell{max-width:1420px;margin:auto;padding:0 24px 45px}.top{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:15px;padding:22px 0;border-bottom:1px solid #30505e}.brand{display:flex;gap:13px;align-items:center}.mark{width:46px;height:46px;display:grid;place-items:center;background:#183947;border:1px solid #558694;border-radius:14px;font-size:25px;color:#f7bb62}.brand b{font-size:22px;letter-spacing:-.04em}.brand small{display:block;font-size:10px;letter-spacing:.18em;color:#a4bec8}.pill{display:inline-flex;align-items:center;gap:9px;border:1px solid #436474;background:#112e3c;border-radius:30px;padding:9px 13px;font-size:12px}.pill:before{content:"";background:#9daeba;width:8px;height:8px;border-radius:50%}.pill.live:before{background:#4fd1a0}.pill.error:before{background:#fa8e88}.pill.wait:before{background:#f7bb62}main{padding-top:28px}.eyebrow{color:#f7bb62;font-size:11px;letter-spacing:.19em;font-weight:800}.intro{display:flex;gap:18px;justify-content:space-between;align-items:end;flex-wrap:wrap;margin:0 0 22px}.muted{color:#a9c3ce;font-size:13px}.btn{background:#f3b962;border:1px solid #f3b962;color:#152a32;border-radius:10px;padding:11px 16px;font-weight:750}.btn.secondary{background:#173a49;border-color:#4b7282;color:#e6f5f8}.btn.danger{background:#953d42;border-color:#bb6468;color:white}.btn:disabled{opacity:.45;cursor:not-allowed}.actions{display:flex;gap:9px;flex-wrap:wrap}.panel{background:linear-gradient(145deg,#143644,#102b39);border:1px solid #315260;border-radius:17px;box-shadow:0 10px 25px #0000001b}.notice{padding:15px 19px;margin-bottom:18px;background:#332f23;border:1px solid #77603a;border-radius:12px;color:#f2dfb7;font-size:13px}.sources{display:flex;gap:9px;flex-wrap:wrap;margin:0 0 16px}.source{display:inline-flex;padding:6px 12px;border-radius:30px;border:1px solid #487363;color:#b6f0d8;background:#143c38;font-size:12px;font-weight:750}.two-col{display:grid;grid-template-columns:1.1fr 1fr;gap:17px}.control,.details,.settings,.graphs{padding:22px;margin-bottom:18px}.buttons{display:flex;gap:11px;flex-wrap:wrap;margin:19px 0}.control-info{display:grid;grid-template-columns:1fr auto;gap:11px 14px;font-size:13px}.control-info span:nth-child(odd){color:#a9c3ce}.control-info strong{text-align:right}.feedback{border-radius:8px;background:#09232f;padding:12px;font-size:13px;min-height:44px}.metrics{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:13px;margin:5px 0 19px}.metric{padding:18px;min-width:0}.metric-label{font-size:12px;color:#aac8d4}.metric-value{font-size:clamp(24px,2.5vw,35px);font-weight:800;letter-spacing:-.04em;margin:15px 0 8px;overflow-wrap:anywhere}.unit{font-size:13px;color:#91b6c4;margin-left:4px;font-weight:500}.metric small{color:#8fafbc;font-size:11px}.graph-head{display:flex;gap:12px;justify-content:space-between;flex-wrap:wrap;align-items:center}.filters{display:flex;gap:7px;flex-wrap:wrap;margin-bottom:13px}.filters button{border:1px solid #456779;background:#163745;border-radius:30px;color:#c5e0e9;padding:8px 13px;font-size:12px}.filters button[aria-pressed="true"]{background:#f3b962;border-color:#f3b962;color:#142b32;font-weight:800}.plots{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:15px}.chart-card{padding:18px;min-width:0}.chart{width:100%;height:200px;display:block}.chart .gridline{stroke:#345665;stroke-width:1;stroke-dasharray:3 6}.chart polyline{fill:none;stroke-width:2.6;stroke-linejoin:round;stroke-linecap:round;vector-effect:non-scaling-stroke}.chart text{fill:#9cb7c3;font-size:11px}.chart .empty{fill:#afc8cf;font-size:13px}.fields{display:grid;grid-template-columns:minmax(210px,2fr) 1fr 1fr 1fr auto;gap:12px;align-items:end}.field label{display:block;color:#b6d2db;font-size:12px;margin-bottom:7px}.field input{width:100%;min-width:0;padding:11px 12px;background:#0a2331;border:1px solid #416475;border-radius:9px;color:white}.details dl{display:grid;grid-template-columns:1fr auto;gap:10px;margin:0;font-size:13px}.details dt{color:#a7c5d1}.details dd{margin:0;text-align:right;overflow-wrap:anywhere;max-width:340px}.diagnostic{padding:12px;margin:17px 0 0;border-radius:9px;background:#09232f;font-size:13px;color:#c5e1e9}footer{margin-top:20px;color:#8eacb9;font-size:12px}@media(max-width:1120px){.metrics{grid-template-columns:repeat(3,minmax(0,1fr))}.fields{grid-template-columns:1fr 1fr}}@media(max-width:760px){.two-col,.plots{grid-template-columns:1fr}.metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.shell{padding:0 14px 30px}}@media(max-width:460px){.metrics,.fields{grid-template-columns:1fr}.metric{padding:16px}.metric-value{margin:9px 0}.control,.details,.settings,.graphs{padding:17px}}@media(prefers-reduced-motion:reduce){*,*:before,*:after{animation:none!important;transition:none!important}}
</style>
<script src="https://cdnjs.cloudflare.com/ajax/libs/mqtt/5.10.4/mqtt.min.js" defer></script>
<script src="./dual-dashboard.js" defer></script>
<script src="./local-controls.js" defer></script>
</head>
<body><div class="shell">
<header class="top"><div class="brand"><div class="mark" aria-hidden="true">◈</div><div><b>IoTMotor</b><small>Comandos + PZEM + ESP32-S3</small></div></div><span class="pill" role="status" id="connection"><span id="connectionText">Desconectado</span></span></header>
<main><div class="intro"><div><span class="eyebrow">DOIS MÓDULOS · MQTT · MONITORAMENTO</span><h1>Comandos e medições<span style="color:#f3b962">.</span></h1><p class="muted">Dados elétricos do ESP32-01; vibração e temperatura do ESP32-S3 (ESP32-02).</p></div><div class="actions"><button class="btn secondary" id="exportBtn" disabled>Exportar CSV</button><button class="btn" id="connectBtn">Conectar ao MQTT</button></div></div>
<div class="notice"><strong>Controle de bancada via rede local.</strong> Na mesma tela servida pelo ESP32, selecione os relés e a sequência e use Ligar/Desligar. O endereço Cloudflare exibe telemetria, mas não aciona saídas pelo MQTT público. Para energizar na bancada: jumper físico GPIO32–GND, relés SEM motor/contatores e parada local acessível. A seleção estrela/triângulo é apenas demonstração de temporização: intertravamento lógico não substitui intertravamento físico, contatores e proteções.</div>
<div class="sources"><span class="source" id="pzemState">PZEM · sem sinal</span><span class="source" id="sensorState">S3 · sem sinal</span></div>
<div class="two-col"><section class="panel control" aria-labelledby="controlTitle"><h2 id="controlTitle">Comandos · ESP32-01</h2><p class="muted">Configure modo de partida e relés abaixo. O ESP32-S3 mede apenas vibração e temperatura.</p><div class="buttons"><button class="btn" type="button" id="startBtn" disabled>Partida antiga indisponível</button><button class="btn secondary" type="button" id="starBtn" disabled title="Exige contatores e intertravamento físico">Protótipo antigo removido</button><button class="btn danger" type="button" id="stopBtn" disabled>Parada antiga indisponível</button></div><div class="control-info"><span>Habilitação física</span><strong id="armValue">Sem telemetria elétrica recente</strong><span>Saídas lógicas K1–K4 · GPIO19/18/23/27</span><strong id="motorValue">Não confirmado</strong><span>Modo</span><strong id="modeValue">—</strong></div><p class="feedback" id="commandFeedback" aria-live="polite">Nenhum comando enviado.</p></section>
<aside class="panel details"><h2>Diagnóstico dos dois módulos</h2><dl><dt>Broker WSS</dt><dd id="brokerValue">—</dd><dt>ESP32 PZEM · status</dt><dd id="commandStatus">—</dd><dt>ESP32 PZEM · última leitura</dt><dd id="commandAge">—</dd><dt>ESP32 PZEM · mensagens</dt><dd id="commandCount">0</dd><dt>ESP32-S3 · status</dt><dd id="sensorStatus">—</dd><dt>ESP32-S3 · última leitura</dt><dd id="sensorAge">—</dd><dt>ESP32-S3 · mensagens</dt><dd id="sensorCount">0</dd></dl><p class="diagnostic" id="diagnostic" aria-live="polite">Conecte ao broker para acompanhar os dois dispositivos.</p></aside></div>
<section aria-label="Grandezas"><div class="graph-head"><h2>Grandezas elétricas e mecânicas</h2><p class="muted">Valores ausentes ou sem dados recentes: —. Aparente e reativa são calculadas apenas com valores disponíveis.</p></div><div class="metrics" id="metrics"></div></section>
<section class="panel graphs"><div class="graph-head"><h2>Gráficos em tempo real</h2><span class="muted">120 amostras por grandeza, por sessão</span></div><div class="filters" role="group" aria-label="Escolher gráficos"><button type="button" data-group="todos" aria-pressed="true">Todos</button><button type="button" data-group="eletrica" aria-pressed="false">Elétricas</button><button type="button" data-group="mecanica" aria-pressed="false">Vibração e temperatura</button></div><div class="plots" id="plots"></div></section>
<section class="panel settings"><h2>Conexão MQTT</h2><p class="muted">A página usa WSS na porta 8081; ambos os ESP32 usam TCP na 1883. Nunca inclua senhas neste site público.</p><form id="connectionForm" class="fields"><div class="field"><label for="broker">URL WebSocket segura</label><input id="broker" type="url" spellcheck="false" autocomplete="off" required></div><div class="field"><label for="prefix">Prefixo</label><input id="prefix" required></div><div class="field"><label for="commandDevice">ID comandos / PZEM</label><input id="commandDevice" required></div><div class="field"><label for="sensorDevice">ID ESP32-S3</label><input id="sensorDevice" required></div><button class="btn secondary" type="submit">Aplicar e conectar</button></form></section>
</main><footer>IoTMotor · dashboard web · bancada de desenvolvimento · leituras não substituem proteções elétricas e de máquina.</footer></div></body></html>
)IOTHTML";
const char IOTMOTOR_DUAL_JS[] PROGMEM = R"IOTDUAL('use strict';
// IoTMotor: esp32-01 PZEM + comandos; esp32-02 MPU6050/DS18B20 (somente leitura).
const $=id=>document.getElementById(id);
const STORE='iotmotor_dashboard_dual_v1';
const DEFAULT={broker:'wss://test.mosquitto.org:8081',prefix:'iotmotor',commandDevice:'esp32-01',sensorDevice:'esp32-02'};
const METRICS=[
 {key:'voltage',label:'Tensão',unit:'V',digits:1,group:'eletrica',color:'#65c7e8',source:'command'},
 {key:'current',label:'Corrente',unit:'A',digits:2,group:'eletrica',color:'#ffc46a',source:'command'},
 {key:'power',label:'Potência ativa',unit:'W',digits:1,group:'eletrica',color:'#e6a179',source:'command'},
 {key:'apparent',label:'Potência aparente',unit:'VA',digits:1,group:'eletrica',color:'#69d3b7',source:'command'},
 {key:'reactive',label:'Potência reativa',unit:'VAr',digits:1,group:'eletrica',color:'#a2adf4',source:'command'},
 {key:'pf',label:'Fator de potência',unit:'',digits:2,group:'eletrica',color:'#83d8a2',source:'command'},
 {key:'frequency',label:'Frequência',unit:'Hz',digits:2,group:'eletrica',color:'#95c8ff',source:'command'},
 {key:'energy',label:'Energia',unit:'kWh',digits:3,group:'eletrica',color:'#d4cf87',source:'command'},
 {key:'vibration',label:'Vibração RMS',unit:'g',digits:3,group:'mecanica',color:'#e6b2d4',source:'sensor'},
 {key:'temperature',label:'Temperatura',unit:'°C',digits:1,group:'mecanica',color:'#f69d84',source:'sensor'}
];
const alias={voltage:['voltage','tensao','v'],current:['current','corrente','i'],power:['power','potencia','w'],pf:['pf','power_factor','fator_potencia','fp'],frequency:['frequency','frequencia','hz'],energy:['energy','energy_kwh','energia','kwh'],vibration:['vibration','vibracao','vib'],temperature:['temperature','temperatura','temp']};
const state={config:{...DEFAULT},client:null,generation:0,connected:false,subscribed:false,group:'todos',
 command:{sample:null,at:0,count:0,status:'—'},sensor:{sample:null,at:0,count:0,status:'—'},
 series:Object.fromEntries(METRICS.map(m=>[m.key,[]])),records:[],pending:null};
function numeric(v){if(v===null||v===undefined||v==='')return null;const n=Number(typeof v==='string'?v.replace(',','.'):v);return Number.isFinite(n)?n:null;}
function field(source,keys){for(const key of keys){const n=numeric(source[key]);if(n!==null)return n;}return null;}
function parseTelemetry(json){
 if(!json||typeof json!=='object'||Array.isArray(json))return null;
 const source=json.data&&typeof json.data==='object'&&!Array.isArray(json.data)?json.data:json;
 const result={deviceId:String(json.device_id||source.device_id||''),demo:json.demo===true||source.demo===true||source.data_source==='simulated',
  dataSource:String(json.data_source||source.data_source||'não informada'),seq:numeric(source.seq),
  benchArmed:source.bench_armed===true,motorOn:typeof source.motor_on==='boolean'?source.motor_on:null,
  mode:typeof source.mode==='string'?source.mode:'—',vibrationPeak:numeric(source.vibration_peak),
  relays:Array.isArray(source.relays)&&source.relays.length===4&&source.relays.every(v=>typeof v==='boolean')?source.relays:null};
 for(const [name,keys]of Object.entries(alias))result[name]=field(source,keys);
 result.apparent=result.voltage!==null&&result.current!==null?result.voltage*result.current:null;
 result.reactive=result.apparent===null?null:result.power!==null?
 Math.sqrt(Math.max(0,result.apparent**2-result.power**2)):
 result.pf!==null?result.apparent*Math.sqrt(Math.max(0,1-result.pf**2)):null;
 return result;
}
function validateConfig(input){
 const broker=String(input.broker||'').trim(),prefix=String(input.prefix||'').trim().replace(/^\/+|\/+$/g,'');
 const commandDevice=String(input.commandDevice||'').trim(),sensorDevice=String(input.sensorDevice||'').trim();
 let url;try{url=new URL(broker);}catch{throw Error('URL WSS inválida.');}
 if(url.protocol!=='wss:'||!url.hostname||url.username||url.password||url.search||url.hash)throw Error('Informe apenas uma URL wss:// válida, sem credenciais.');
 if(!/^[a-zA-Z0-9_-]+(?:\/[a-zA-Z0-9_-]+)*$/.test(prefix))throw Error('Prefixo de tópicos inválido.');
 if(!/^[a-zA-Z0-9_-]+$/.test(commandDevice)||!/^[a-zA-Z0-9_-]+$/.test(sensorDevice)||commandDevice===sensorDevice)throw Error('Informe IDs distintos e válidos para os dois ESP32.');
 return {broker:url.toString(),prefix,commandDevice,sensorDevice};
}
function text(id,value){$(id).textContent=String(value);}
function diag(message){text('diagnostic',message);}
function pill(message,kind=''){text('connectionText',message);$('connection').className=`pill ${kind}`.trim();$('connectBtn').textContent=state.client?'Desconectar':'Conectar ao MQTT';}
function topic(device,kind){return `${state.config.prefix}/${device}/${kind}`;}
function freshness(which){return state.connected&&state.subscribed&&state[which].at>0&&Date.now()-state[which].at<10000;}
function valueFor(metric){const source=state[metric.source];return freshness(metric.source)&&source.sample?source.sample[metric.key]:null;}
function updateControl(){
 const active=freshness('command');const s=state.command.sample;
 $('startBtn').disabled=true; // Old single-relay prototype retired.
 $('stopBtn').disabled=true; // LAN-only control lives in local-controls.js.
 $('starBtn').disabled=true; // um rele nao executa estrela-triangulo com intertravamento
 text('armValue',active?(s.benchArmed?'Jumper local presente':'Sem jumper local'):'Sem telemetria elétrica recente');
 text('motorValue',active&&Array.isArray(s?.relays)?s.relays.map((on,i)=>`K${i+1}:${on?'L':'D'}`).join(' · '):'Sem estados lógicos recentes');
 text('modeValue',active?s.mode:'—');
}
function svg(tag,attrs={},content){const n=document.createElementNS('http://www.w3.org/2000/svg',tag);for(const [key,v]of Object.entries(attrs))n.setAttribute(key,String(v));if(content!==undefined)n.textContent=String(content);return n;}
function drawChart(target,metric){
 const values=state.series[metric.key].map(entry=>entry.v);
 target.replaceChildren();
 if(!values.length){target.append(svg('text',{x:320,y:105,'text-anchor':'middle',class:'empty'},'Sem leitura disponível'));return;}
 let min=Math.min(...values),max=Math.max(...values);const pad=Math.max((max-min)*.12,Math.abs(max)*.01,.01);min-=pad;max+=pad;
 for(let i=0;i<4;i++){const y=20+160*i/3;target.append(svg('line',{x1:53,x2:633,y1:y,y2:y,class:'gridline'}));target.append(svg('text',{x:45,y:y+4,'text-anchor':'end'},(max-(max-min)*i/3).toFixed(metric.digits>2?2:metric.digits)));}
 const points=values.map((v,i)=>`${(56+(values.length===1?280:570*i/(values.length-1))).toFixed(1)},${(180-(v-min)/(max-min)*160).toFixed(1)}`).join(' ');
 target.append(svg('polyline',{points,stroke:metric.color}));target.append(svg('text',{x:56,y:209},'Mais antigo'));target.append(svg('text',{x:631,y:209,'text-anchor':'end'},'Mais recente'));
}
function renderCharts(){const root=$('plots');root.replaceChildren();for(const metric of METRICS.filter(m=>state.group==='todos'||m.group===state.group)){
 const article=document.createElement('article');article.className='panel chart-card';
 const heading=document.createElement('h3');heading.textContent=`${metric.label}${metric.unit?' ('+metric.unit+')':''}`;
 const graphic=svg('svg',{viewBox:'0 0 650 215',class:'chart',role:'img','aria-label':`Gráfico de ${metric.label}`});
 drawChart(graphic,metric);article.append(heading,graphic);root.append(article);
}}
function buildCards(){const root=$('metrics');for(const m of METRICS){
 const item=document.createElement('article');item.className='panel metric';
 const label=document.createElement('div');label.className='metric-label';label.textContent=m.label;
 const value=document.createElement('div');value.className='metric-value';
 const number=document.createElement('span');number.id=`value-${m.key}`;number.textContent='—';
 const unit=document.createElement('span');unit.className='unit';unit.textContent=m.unit;value.append(number,unit);
 const detail=document.createElement('small');detail.id=`hint-${m.key}`;detail.textContent='Sem leitura';item.append(label,value,detail);root.append(item);
}}
function render(){
 for(const m of METRICS){const value=valueFor(m),source=state[m.source];text(`value-${m.key}`,value===null?'—':value.toFixed(m.digits));
 text(`hint-${m.key}`,value===null?'Sem leitura atual':source.sample.demo?'Simulado — não é medição':m.source==='command'?'ESP32 PZEM-004T':'ESP32-S3 sensores');}
 text('pzemState',freshness('command')?(state.command.sample.demo?'SIMULADO':'PZEM · dados recebidos'):'PZEM · sem sinal');
 text('sensorState',freshness('sensor')?(state.sensor.sample.demo?'SIMULADO':'S3 · dados recebidos'):'S3 · sem sinal');
 text('commandCount',state.command.count);text('sensorCount',state.sensor.count);
 text('commandStatus',state.command.status);text('sensorStatus',state.sensor.status);
 text('commandAge',state.command.at?new Date(state.command.at).toLocaleTimeString('pt-BR'):'—');
 text('sensorAge',state.sensor.at?new Date(state.sensor.at).toLocaleTimeString('pt-BR'):'—');
 $('exportBtn').disabled=!state.records.length;updateControl();renderCharts();
}
function reset(){state.command={sample:null,at:0,count:0,status:'—'};state.sensor={sample:null,at:0,count:0,status:'—'};
 state.series=Object.fromEntries(METRICS.map(m=>[m.key,[]]));state.records=[];state.pending=null;state.subscribed=false;
 text('commandFeedback','Nenhum comando enviado.');render();}
function disconnect(){const old=state.client;state.generation++;state.client=null;state.connected=false;state.subscribed=false;if(old)old.end(true);reset();pill('Desconectado');diag('Desconectado.');}
function ingest(which,raw,packet){
 if(packet?.retain===true){diag(`Telemetria retida antiga de ${which==='command'?'ESP32 PZEM':'ESP32-S3'} ignorada.`);return false;}
 let json;try{json=JSON.parse(raw);}catch{diag('Mensagem MQTT recebida, mas JSON inválido.');return false;}
 const expected=which==='command'?state.config.commandDevice:state.config.sensorDevice;
 const sample=parseTelemetry(json);if(!sample||sample.deviceId!==expected){diag('Mensagem descartada: device_id diferente do tópico.');return false;}
 const hasFields=METRICS.some(m=>m.source===which&&sample[m.key]!==null);
 if(which==='command'&&!hasFields&&sample.motorOn===null&&!sample.relays){diag('Mensagem do módulo de comandos sem grandezas nem estado válido.');return false;}
 state[which].sample=sample;state[which].at=Date.now();state[which].count++;
 for(const m of METRICS.filter(m=>m.source===which)){
  if(sample[m.key]!==null){const arr=state.series[m.key];arr.push({t:state[which].at,v:sample[m.key]});if(arr.length>120)arr.shift();}
 }
 state.records.push({at:new Date(state[which].at).toISOString(),deviceId:expected,...sample});if(state.records.length>500)state.records.shift();
 if(which==='command'&&state.pending&&state.command.at>=state.pending.at&&sample.motorOn===state.pending.target){
  text('commandFeedback',`ESP32 informou saída ${sample.motorOn?'ligada':'desligada'}; não confirma contatores ou motor físico.`);state.pending=null;
 }
 diag(`Recebendo ${which==='command'?'dados do PZEM / comandos':'vibração e temperatura do S3'} · seq ${sample.seq??'—'}.`);render();return true;
}
function connect(){
 let config;try{config=validateConfig({broker:$('broker').value,prefix:$('prefix').value,commandDevice:$('commandDevice').value,sensorDevice:$('sensorDevice').value});}
 catch(e){diag(e.message);return;}
 if(!window.mqtt||typeof window.mqtt.connect!=='function'){pill('MQTT.js indisponível','error');diag('Biblioteca MQTT.js não carregou; confira o acesso ao CDN.');return;}
 if(state.client)disconnect();state.config=config;
 try{localStorage.setItem(STORE,JSON.stringify(config));}catch{}
 text('brokerValue',config.broker);const generation=++state.generation;let client;
 try{client=window.mqtt.connect(config.broker,{clientId:`iotmotor_dual_${Math.random().toString(36).slice(2,11)}`,clean:true,protocolVersion:4,reconnectPeriod:4000,connectTimeout:10000,keepalive:30,resubscribe:true});}
 catch(e){pill('Falha MQTT','error');diag(e.message);return;}
 state.client=client;reset();pill('Conectando…','wait');diag(`Conectando ${config.broker}; dispositivos ${config.commandDevice} e ${config.sensorDevice}.`);
 const active=()=>state.client===client&&state.generation===generation;
 client.on('connect',()=>{
  if(!active())return;state.connected=true;pill('Broker conectado','live');
  const topics=[topic(config.commandDevice,'telemetry'),topic(config.commandDevice,'status'),topic(config.sensorDevice,'telemetry'),topic(config.sensorDevice,'status')];
  client.subscribe(topics,{qos:0},err=>{
   if(!active())return;state.subscribed=!err;diag(err?`Conectado, erro de assinatura: ${err.message}`:`Broker conectado. Aguardando ${topics[0]} e ${topics[2]}.`);updateControl();
  });
 });
 client.on('message',(destination,payload,packet)=>{
  if(!active())return;
  const which=destination.startsWith(`${config.prefix}/${config.commandDevice}/`)?'command':destination.startsWith(`${config.prefix}/${config.sensorDevice}/`)?'sensor':null;
  if(!which)return;
  if(destination===topic(config[which==='command'?'commandDevice':'sensorDevice'],'status')){
   state[which].status=payload.toString('utf8').slice(0,80);render();return;
  }
  if(destination===topic(config[which==='command'?'commandDevice':'sensorDevice'],'telemetry'))ingest(which,payload.toString('utf8'),packet);
 });
 client.on('reconnect',()=>{if(!active())return;state.connected=false;state.subscribed=false;pill('Reconectando…','wait');render();});
 client.on('offline',()=>{if(!active())return;state.connected=false;state.subscribed=false;pill('Broker indisponível','error');diag('Falha WSS: verifique rede, broker e porta 8081.');render();});
 client.on('error',err=>{if(active())diag(`MQTT: ${String(err.message||err).slice(0,140)}`);});
 client.on('close',()=>{if(!active())return;state.connected=false;state.subscribed=false;pill('Conexão encerrada','error');render();});
}
function command(kind,mode){ /* Retired GPIO2 prototype: never publish control on a public broker. */ }
function exportCsv(){
 if(!state.records.length)return;
 const keys=METRICS.map(m=>m.key);
 const lines=[['received_at','device_id','demo','motor_on','bench_armed',...keys].join(',')];
 for(const row of state.records)lines.push([row.at,row.deviceId,row.demo,row.motorOn??'',row.benchArmed,...keys.map(k=>row[k]??'')].join(','));
 const blob=new Blob(['\ufeff'+lines.join('\r\n')],{type:'text/csv;charset=utf-8'});
 const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`iotmotor-2-modulos-${new Date().toISOString().slice(0,10)}.csv`;a.click();
 setTimeout(()=>URL.revokeObjectURL(url),1000);
}
function init(){
 try{const saved=JSON.parse(localStorage.getItem(STORE)||'null');if(saved)state.config=validateConfig(saved);}catch{}
 $('broker').value=state.config.broker;$('prefix').value=state.config.prefix;
 $('commandDevice').value=state.config.commandDevice;$('sensorDevice').value=state.config.sensorDevice;
 text('brokerValue',state.config.broker);buildCards();render();
 $('connectBtn').addEventListener('click',()=>state.client?disconnect():connect());
 $('connectionForm').addEventListener('submit',event=>{event.preventDefault();connect();});
 $('startBtn').addEventListener('click',()=>command('start','direct'));
 $('stopBtn').addEventListener('click',()=>command('stop','manual_stop'));
 $('exportBtn').addEventListener('click',exportCsv);
 for(const button of document.querySelectorAll('[data-group]'))button.addEventListener('click',()=>{
  state.group=button.dataset.group;for(const b of document.querySelectorAll('[data-group]'))b.setAttribute('aria-pressed',String(button===b));renderCharts();
 });
 setInterval(()=>{
  if(state.pending&&Date.now()-state.pending.at>6500){text('commandFeedback','Sem confirmação do ESP32. Não pressuponha que a saída esteja desligada.');state.pending=null;}
  if(state.connected&&state.subscribed&&(!freshness('command')||!freshness('sensor')))
   diag(`Broker conectado; ${!freshness('command')?'sem dados recentes do PZEM/comandos':''}${!freshness('command')&&!freshness('sensor')?' e ':''}${!freshness('sensor')?'sem dados recentes do ESP32-S3':''}.`);
  render();
 },1500);
}
if(typeof document!=='undefined')init();
if(typeof module!=='undefined'&&module.exports)module.exports={parseTelemetry,validateConfig,METRICS};
)IOTDUAL";
const char IOTMOTOR_LOCAL_JS[] PROGMEM = R"IOTLOCAL('use strict';
/* A mesma tela funciona no Cloudflare (monitoramento) e servida pelo ESP32 (controle LAN).
 * Nunca publica comandos num broker MQTT público nem faz HTTP misto a partir de HTTPS. */
(() => {
  const $=id=>document.getElementById(id);
  const control=document.querySelector('.control');
  if (!control) return;
  for (const id of ['startBtn','starBtn','stopBtn']) { const node=$(id); if(node) node.hidden=true; }
  const onLan=location.protocol==='http:' && (/^(?:10|192\.168|172\.(?:1[6-9]|2\d|3[01]))\./.test(location.hostname)||location.hostname==='modulo1.local');
  const box=document.createElement('section');
  box.style.cssText='border:1px solid #50778a;border-radius:12px;padding:16px;background:#0a2936;margin:12px 0';
  box.innerHTML=`<h3 style="font-size:18px">Partida e seleção de relés · K1–K4</h3>
    <p class="muted" id="controlHint">Controle local do ESP32. As saídas são estados lógicos, não confirmação de contatores.</p>
    <div class="field"><label for="startMode">Tipo de partida</label><select id="startMode" style="padding:9px;background:#102e3b;color:white;border:1px solid #608697;border-radius:8px"><option value="direct">Direta · relés selecionados</option><option value="sequence">Sequência de bancada · principal, estrela e triângulo</option></select></div>
    <div id="directSelect" style="margin:12px 0"><span class="muted">Relés acionados na partida direta:</span><div id="directChannels" class="buttons"></div></div>
    <div id="sequenceSelect" hidden style="margin:12px 0"><p class="muted">Sequência apenas para testes com relés desconectados de motor/contatores. Não substitui intertravamento físico.</p><div class="fields" style="display:flex;flex-wrap:wrap"><label>Principal <select id="mainRelay"></select></label><label>Estrela <select id="starRelay"></select></label><label>Triângulo <select id="deltaRelay"></select></label><label>Tempo estrela (s) <input id="starSeconds" type="number" min="2" max="30" step="1" value="5" style="width:70px"></label></div></div>
    <div class="buttons"><button id="profileStart" class="btn" type="button" disabled>▶ Ligar / Iniciar</button><button id="profileStop" class="btn danger" type="button" disabled>■ Desligar todos</button></div>
    <p id="profileStatus" class="feedback" aria-live="polite">Aguardando estado do ESP32.</p>
    <div class="muted" id="relayStatuses">K1 GPIO19 · K2 GPIO18 · K3 GPIO23 · K4 GPIO27</div>
    <pre id="physicalLcd" style="white-space:pre;font:14px/1.4 monospace;max-width:100%;overflow:auto;background:#051b26;padding:12px;border:1px solid #466878;border-radius:9px;margin:12px 0">LCD 20×4 · aguardando dados</pre>
    <div id="remoteEntry" hidden><label class="muted" for="localIp">IP informado na primeira linha do LCD físico</label><div class="buttons"><input id="localIp" inputmode="decimal" placeholder="192.168.1.100" style="background:#051b26;border:1px solid #466878;color:#fff;padding:9px;border-radius:8px"><button id="sameTab" class="btn secondary" type="button">Abrir esta tela no ESP32 (mesma aba)</button></div><p class="muted">No endereço HTTPS do Cloudflare os comandos locais não podem ser executados. O mesmo dashboard, servido pelo IP do ESP32, controla os relés sem abrir outra aba.</p></div>`;
  control.append(box);
  const pins=[19,18,23,27];let busy=false, snapshot=null, lastAt=0, mqtt=null;
  const privateIp=value=>{if(typeof value!=='string'||!/^\d{1,3}(?:\.\d{1,3}){3}$/.test(value))return null;const p=value.split('.').map(Number);if(p.some(x=>x>255))return null;return (p[0]===10||p[0]===192&&p[1]===168||p[0]===172&&p[1]>=16&&p[1]<=31)?p.join('.'):null;};
  const direct=$('directChannels');
  pins.forEach((pin,i)=>{const label=document.createElement('label');label.style.cssText='display:inline-flex;align-items:center;gap:5px';const input=document.createElement('input');input.type='checkbox';input.value=String(i+1);input.checked=i===0;input.className='directRelay';label.append(input,document.createTextNode(`K${i+1} · GPIO${pin}`));direct.append(label);});
  for(const id of ['mainRelay','starRelay','deltaRelay']){const select=$(id);pins.forEach((pin,i)=>select.add(new Option(`K${i+1} · GPIO${pin}`,String(i+1))));}
  $('mainRelay').value='1';$('starRelay').value='2';$('deltaRelay').value='3';
  $('startMode').addEventListener('change',()=>{const seq=$('startMode').value==='sequence';$('directSelect').hidden=seq;$('sequenceSelect').hidden=!seq;});
  function show(data){snapshot=data;lastAt=Date.now();const relays=data.reles||data.relays;const valid=Array.isArray(relays)&&relays.length===4;
    $('relayStatuses').textContent=valid?relays.map((on,i)=>`K${i+1} GPIO${pins[i]}: ${on?'LIGADO':'desligado'}`).join(' · '):'Sem estados válidos dos relés';
    const rows=data.lcd||data.lcd_lines;if(Array.isArray(rows)&&rows.length===4)$('physicalLcd').textContent=rows.map(x=>String(x).slice(0,20).padEnd(20)).join('\n');
    if(onLan){$('profileStatus').textContent=`${data.partida_etapa||'Parado'} · ${data.armado?'habilitação física presente':'GPIO32 sem jumper: partida bloqueada'}`;
      $('profileStart').disabled=busy||!valid||!data.armado||relays.some(Boolean);
      $('profileStop').disabled=busy||!valid;
    }
  }
  async function poll(){if(!onLan||busy)return;try{const res=await fetch('/dados',{cache:'no-store'});if(!res.ok)throw Error('HTTP '+res.status);show(await res.json());}catch(e){$('profileStart').disabled=true;$('profileStop').disabled=true;$('profileStatus').textContent='ESP32 não respondeu: '+e.message;}}
  async function post(path,params){if(busy)return;busy=true;$('profileStart').disabled=true;$('profileStop').disabled=true;$('profileStatus').textContent='Pedido enviado; aguardando resposta e estado dos pinos…';try{const res=await fetch(path,{method:'POST',body:new URLSearchParams(params),cache:'no-store'});const message=await res.text();if(!res.ok)throw Error(`${res.status}: ${message}`);$('profileStatus').textContent='Comando recebido pelo ESP32; conferindo saídas…';}catch(e){$('profileStatus').textContent='Falha: '+e.message;}finally{busy=false;await poll();}}
  $('profileStart').addEventListener('click',()=>{if(!onLan||!snapshot?.armado)return;const mode=$('startMode').value;let args={modo:mode};if(mode==='direct'){let mask=0;document.querySelectorAll('.directRelay:checked').forEach(x=>mask|=1<<(Number(x.value)-1));if(!mask){$('profileStatus').textContent='Selecione pelo menos um relé.';return;}args.mascara=String(mask);}else{const chosen=[$('mainRelay').value,$('starRelay').value,$('deltaRelay').value];if(new Set(chosen).size!==3){$('profileStatus').textContent='Principal, estrela e triângulo devem ser relés distintos.';return;}Object.assign(args,{principal:chosen[0],estrela:chosen[1],triangulo:chosen[2],tempo:$('starSeconds').value});}if(window.confirm('TESTE EM BANCADA SEM MOTOR NEM CONTATORES: confirmar partida dos relés selecionados?'))post('/partida',args);});
  $('profileStop').addEventListener('click',()=>onLan&&post('/parar',{}));
  if(onLan){$('controlHint').textContent='Controle na mesma página, servido pelo ESP32 na rede local. Jumper GPIO32–GND obrigatório para energizar; desligamento sempre disponível.';poll();setInterval(poll,1200);return;}
  $('remoteEntry').hidden=false;$('profileStatus').textContent='Monitoramento remoto: controle somente na mesma tela servida pelo ESP32 na rede local.';
  $('sameTab').addEventListener('click',()=>{const ip=privateIp($('localIp').value.trim());if(!ip){$('profileStatus').textContent='Informe um IPv4 privado válido conferido no LCD.';return;}if(window.confirm(`Abrir http://${ip}/ nesta mesma aba? Confirme o IP exibido no LCD do ESP32.`))location.assign(`http://${ip}/`);});
  if(!window.mqtt?.connect)return;
  let config={};try{config=JSON.parse(localStorage.getItem('iotmotor_dashboard_dual_v1')||'{}')||{};}catch{}
  let broker;try{broker=new URL(config.broker||'wss://test.mosquitto.org:8081');}catch{return;}
  if(broker.protocol!=='wss:'||broker.username||broker.password)return;
  const prefix=String(config.prefix||'iotmotor'),device=String(config.commandDevice||'esp32-01');
  if(!/^[\w-]+(?:\/[\w-]+)*$/.test(prefix)||!/^[\w-]+$/.test(device))return;
  const telemetryTopic=`${prefix}/${device}/telemetry`;
  mqtt=window.mqtt.connect(broker.toString(),{clientId:`iotmotor_local_${Math.random().toString(36).slice(2,11)}`,clean:true,reconnectPeriod:4000});
  mqtt.on('connect',()=>mqtt.subscribe(telemetryTopic));
  mqtt.on('message',(topic,buffer,packet)=>{if(topic!==telemetryTopic||packet?.retain)return;let d;try{d=JSON.parse(buffer.toString());}catch{return;}if(d?.device_id!==device||!Array.isArray(d.relay_pins)||d.relay_pins.join(',')!=='19,18,23,27')return;const ip=privateIp(d.wifi_ip)||privateIp((String(d.lcd?.[0]||'').match(/IP:\s*(\d{1,3}(?:\.\d{1,3}){3})/)||[])[1]);if(ip&&!$('localIp').value)$('localIp').value=ip;show({reles:d.relays,lcd:d.lcd});$('profileStatus').textContent='Monitoramento disponível. Confirme o IP físico e abra esta mesma tela na rede local para comandar.';});
})();
)IOTLOCAL";
